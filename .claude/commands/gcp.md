---
description: GCP cloud infrastructure — GKE, Cloud Run, Terraform modules, cloud-native patterns. Usage: /gcp <what to build or design>
---

Help with GCP infrastructure for: $ARGUMENTS

Read `engineering-team/gcp-cloud-architect/SKILL.md` and apply the relevant workflow:
- If designing infrastructure → follow the GCP Architecture workflow
- If writing Terraform → follow the Terraform Modules workflow
- If deploying containers → follow the GKE or Cloud Run workflow
- If setting up cloud-native patterns → follow the Cloud-Native Architecture workflow

Ask what to build if $ARGUMENTS is empty.
