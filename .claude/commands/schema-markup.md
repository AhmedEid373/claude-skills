---
description: Implement JSON-LD structured data and schema markup for rich results. Usage: /schema-markup <page type or content>
---

Implement schema markup for: $ARGUMENTS

Read `marketing-skill/schema-markup/SKILL.md` and execute the Schema Markup workflow:
1. Identify the appropriate schema type (FAQ, Product, HowTo, Article, Organization, BreadcrumbList, etc.)
2. Generate valid JSON-LD structured data
3. Validate against Google's Rich Results requirements
4. Provide implementation instructions (where to add in HTML/Next.js)
5. Test with Google Rich Results Test guidance
6. Flag any Search Console structured data errors if provided

Ask what page or content type to add schema markup to if $ARGUMENTS is empty.
