---
description: Stripe payments — subscriptions, billing portal, webhooks, free trials. Usage: /stripe <what to build>
---

Help with Stripe payment integration for: $ARGUMENTS

Read `engineering-team/stripe-integration-expert/SKILL.md` and apply the relevant workflow:
- If building subscriptions → follow the Subscription Plans workflow (free trial, monthly/yearly, plan upgrades)
- If setting up webhooks → follow the Webhook Handler workflow
- If building a billing portal → follow the Customer Portal workflow
- If handling one-time payments → follow the Checkout Session workflow
- If testing payments → follow the Test Mode workflow (test card numbers, scenarios)

Ask what payment feature to build if $ARGUMENTS is empty.
