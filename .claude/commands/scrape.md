---
description: Scrape websites, extract structured data, and automate browser workflows with Playwright. Usage: /scrape <website or task>
---

Build a web scraper or browser automation for: $ARGUMENTS

Read `engineering/browser-automation/SKILL.md` and apply the relevant workflow:
- If scraping a website → follow the Web Scraping Patterns workflow (tables, listings, search results)
- If site uses JavaScript/SPA → follow the Dynamic Content Extraction workflow
- If automating a multi-step workflow → follow the Browser Workflow Automation workflow (login, fill forms, download)
- If capturing screenshots/PDFs → follow the Screenshot Capture workflow
- If building a data pipeline → follow the Repeatable Data Pipeline workflow
- If bypassing bot detection → follow the Anti-Detection Patterns workflow

Ask what site or task to automate if $ARGUMENTS is empty.
