---
description: Build React/Next.js frontends with TypeScript and Tailwind CSS. Usage: /frontend <what to build>
---

Help with frontend development for: $ARGUMENTS

Read `engineering-team/senior-frontend/SKILL.md` and apply the relevant workflow:
- If building a component → follow the Component Generator workflow
- If optimizing an existing app → follow the Bundle Analyzer workflow
- If starting a new frontend → follow the Frontend Scaffolder workflow

Ask what to build if $ARGUMENTS is empty.
