---
description: Design REST APIs, auth flows, and backend systems (Node.js, Python, FastAPI). Usage: /backend <what to build>
---

Help with backend development for: $ARGUMENTS

Read `engineering-team/senior-backend/SKILL.md` and apply the relevant workflow:
- If designing APIs → follow the API Scaffolder workflow
- If working on auth → follow the Authentication Flow workflow
- If optimizing DB → follow the Database Migration workflow
- If load testing → follow the API Load Tester workflow

Ask what to build if $ARGUMENTS is empty.
