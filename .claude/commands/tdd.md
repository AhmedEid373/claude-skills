---
description: Test-Driven Development — write tests first, implement to pass. Usage: /tdd <feature or function to implement>
---

Apply TDD methodology for: $ARGUMENTS

Read `engineering-team/tdd-guide/SKILL.md` and follow the TDD cycle:
1. **RED** — Write failing tests for the requirement
2. **GREEN** — Write the minimum code to make tests pass
3. **REFACTOR** — Clean up while keeping tests green

Apply appropriate testing patterns:
- Unit tests for individual functions/classes
- Integration tests for API endpoints or DB interactions
- E2E tests for critical user flows

Ask what feature to implement with TDD if $ARGUMENTS is empty.
