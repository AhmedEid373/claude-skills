---
description: AWS infrastructure design — VPC, ECS, RDS, Lambda, S3, IAM, and more. Usage: /aws <what to build or design>
---

Help with AWS infrastructure for: $ARGUMENTS

Read `engineering-team/aws-solution-architect/SKILL.md` and apply the relevant workflow:
- If designing infrastructure → follow the AWS Architecture workflow
- If setting up networking → follow the VPC and Networking workflow
- If deploying containers → follow the ECS/EKS workflow
- If setting up serverless → follow the Lambda workflow
- If configuring security → follow the IAM and Security workflow

Ask what to build if $ARGUMENTS is empty.
