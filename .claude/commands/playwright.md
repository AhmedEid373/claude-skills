---
description: Playwright E2E testing — generate, fix, review, and migrate browser tests. Usage: /playwright <what to test or fix>
---

Help with Playwright testing for: $ARGUMENTS

Read `engineering-team/playwright-pro/SKILL.md` and apply the relevant sub-skill:
- If setting up Playwright → run `/pw:init` workflow (config, CI, first test)
- If generating tests → run `/pw:generate` workflow (from user story, URL, or component)
- If fixing flaky tests → run `/pw:fix` workflow (diagnose and repair)
- If reviewing test quality → run `/pw:review` workflow (anti-patterns, coverage gaps)
- If migrating from Cypress/Selenium → run `/pw:migrate` workflow
- If analyzing coverage → run `/pw:coverage` workflow

Ask what to test or fix if $ARGUMENTS is empty.
