---
description: Write, optimize, and debug SQL queries and ORM code. Usage: /db-query <your question or slow query>
---

Help with SQL and database queries for: $ARGUMENTS

Read `engineering/sql-database-assistant/SKILL.md` and apply the relevant workflow:
- If writing a new query → follow the Natural Language to SQL workflow
- If optimizing a slow query → follow the Query Optimization workflow (EXPLAIN analysis, index suggestions)
- If working with an ORM → follow the ORM Integration workflow (Prisma, Drizzle, SQLAlchemy, TypeORM)
- If exploring a schema → follow the Schema Exploration workflow

Ask what query help is needed if $ARGUMENTS is empty.
