---
description: Azure cloud infrastructure — AKS, App Service, Bicep/ARM templates, landing zones. Usage: /azure <what to build or design>
---

Help with Azure infrastructure for: $ARGUMENTS

Read `engineering-team/azure-cloud-architect/SKILL.md` and apply the relevant workflow:
- If designing infrastructure → follow the Azure Architecture workflow
- If writing IaC → follow the ARM/Bicep Templates workflow
- If setting up landing zones → follow the Landing Zone workflow
- If deploying containers → follow the AKS workflow

Ask what to build if $ARGUMENTS is empty.
