---
description: Analyze and optimize existing database schemas — normalization, indexing, migrations. Usage: /db-schema <describe your schema or problem>
---

Help with database design and optimization for: $ARGUMENTS

Read `engineering/database-designer/SKILL.md` and apply the relevant workflow:
- If designing from scratch → follow the Normalization Analysis workflow (1NF through BCNF)
- If optimizing performance → follow the Index Optimization workflow
- If migrating data → follow the Zero-Downtime Migration workflow (Expand-Contract Pattern)
- If choosing a database → follow the Multi-Database Decision Matrix (PostgreSQL, MySQL, MongoDB, Redis)

Ask what to analyze if $ARGUMENTS is empty.
