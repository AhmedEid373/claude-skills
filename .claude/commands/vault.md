---
description: HashiCorp Vault and cloud secret store setup, policies, and dynamic secrets. Usage: /vault <what to configure>
---

Help with secret vault infrastructure for: $ARGUMENTS

Read `engineering/secrets-vault-manager/SKILL.md` and apply the relevant workflow:
- If setting up Vault → follow the HashiCorp Vault HA setup workflow
- If writing policies → follow the Policy Authoring workflow
- If configuring auth → follow the Auth Method Configuration workflow
- If setting up dynamic secrets → follow the Dynamic Secrets workflow (DB credentials, IAM, SSH CA)
- If setting up rotation → follow the Secret Rotation Workflow by secret type
- If auditing → follow the Audit Logging and Compliance Reporting workflow

Ask what to configure if $ARGUMENTS is empty.
